#define _CRT_SECURE_NO_WARNINGS
#include "ebentoak.h"
#include "graphics.h"
#include "imagen.h"
#include "soinua.h"
#include "text.h"
#include "irudiak.h" 
#include "menua.h"
#include "jolasa.h"
#include <stdio.h>
#include "OurTypes.h"
#include <SDL.h>
#include <SDL_image.h>


/*


if (ebentu == SAGU_BOTOIA_EZKERRA)
{
    pos = saguarenPosizioa();
    if ((pos.x > 555) && (pos.x < 341) && (pos.y > 1362) && (pos.y < 649))
    {
        iparamerika(); //Ipar Amerika
        return 0;
    }
    if ((pos.x > 520) && (pos.x < 520 + 266) && (pos.y > 564) && (pos.y < 564 + 73))
    {
        hegoamerika();    //Hego Amerika
        irten = -1;
    }
    if ((pos.x > 520) && (pos.x < 520 + 266) && (pos.y > 453) && (pos.y < 453 + 73))
    {
        europa();    //Europa
        irten = -1;
    }
    if ((pos.x > 520) && (pos.x < 520 + 266) && (pos.y > 453) && (pos.y < 453 + 73))
    {
        afrika(); //Afrika
        irten = -1;
    }
    if ((pos.x > 520) && (pos.x < 520 + 266) && (pos.y > 336) && (pos.y < 336 + 73))
    {
        asia(); //Asia
        irten = -1;
    }
    if ((pos.x > 520) && (pos.x < 520 + 266) && (pos.y > 336) && (pos.y < 336 + 73))
    {
        ozeania(); //Ozeania
        irten = -1;
    }
    */
